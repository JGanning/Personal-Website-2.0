import React, { Component } from "react";

export class ExperimentsDescription extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <div>
        This is the tab where I experiment with new react concepts and ideas
        before I implement them into a bigger project
      </div>
    );
  }
}

export default ExperimentsDescription;
